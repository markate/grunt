define([
], function () {
    angular.module('operator').register.controller('pageController', [
        '$scope',
        '$location',
        '$route',
        '$http',
        function ($scope, $location, $route,$http) {

        }
    ]);

});
